# name: phase3_crop_heads
# max_epochs: 40
# patience: 5
# learning_rate: 1e-3
# weight_decay: 1e-4
# scheduler: cosine
# loss: weighted_cross_entropy
# optimizer: adamw

train:
  phase3:
    name: phase3_crop_heads

    max_epochs: 40
    patience: 5

    learning_rate: 1e-3
    weight_decay: 1e-4

    scheduler: cosine
    optimizer: adamw

    loss:
      name: weighted_cross_entropy